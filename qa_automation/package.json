{
  "name": "digital-signage-qa",
  "version": "1.0.0",
  "description": "Automated E2E tests for Digital Signage project",
  "scripts": {
    "test": "playwright test -c playwright.config.ts",
    "test:prod": "playwright test -c playwright.config.ts",
    "test:local": "playwright test -c playwright.local.config.ts",
    "test:visual": "playwright test -c playwright.visual.config.ts",
    "test:ui": "playwright test -c playwright.config.ts --ui",
    "test:debug": "playwright test -c playwright.config.ts --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.41.0",
    "@types/node": "^20.11.0",
    "typescript": "^5.3.3"
  }
}
